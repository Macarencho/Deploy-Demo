{
  "dependencies": {
    "mongoose-slug-generator": "^1.0.4"
  },
  "scripts": {
    "start": "start cmd /k \"npm run backend\" && start cmd /k \"npm run frontend\"",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.1",
    "npm-run-all": "^4.1.5"
  }
}
